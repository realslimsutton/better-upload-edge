import { MultipartFilesUploader } from '@/components/multipart-files-uploader';

export default function Page() {
  return (
    <main className="flex h-screen items-center justify-center">
      <MultipartFilesUploader />
    </main>
  );
}
